import requests
import time
import json

# Acesso ao site dos dados dos voos
#https://pt.flightaware.com/live/flight/GLO1906


#voo = text
#res = requests.get ("https://pt.flightaware.com/live/flight/{voo}")


#Ler as mensagens que estão chegando no boot
# propriedade do telegram que serão utiliadas: text, first_name, id, update_id
while True:
    token = ""
    url_base = f'https://api.telegram.org/bot{token}/getUpdates'
    resultado = requests.get(url_base) 
    print(resultado.json())
    time. sleep(10) 